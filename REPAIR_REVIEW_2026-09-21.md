# 修复后复查报告

结论：已有实质修复，但尚不能验收为全部完成；存在新增高风险缺陷及未完成项。本次只审查和测试，未修改业务代码、未执行数据库同步或真实写操作。

## 已执行验证

- 学生端测试：55/55 通过。
- 管理端测试：10/10 通过。
- 后端 `mvn clean test`：237 项，失败 0、错误 0、跳过 0；统计本轮清理后的报告。
- 两端生产构建通过；仍有超过 500KB 的分包提示。
- 用 PowerShell 解析器验证同步脚本中导入命令的语法：`RedirectionNotSupported`，`<` 运算符不支持。没有执行删表或导入。
- 用项目当前 Vue 的 `ref/effect` 复现 AI 消息写法：修改原始对象后，响应式订阅没有重跑，渲染值仍为空。
- 本轮未重新运行浏览器全流程。Docker API 连接失败，真实 MySQL/Redis/AI/WebSocket/上传联调未验证；上轮浏览器通过不能代替这轮改动的验收。

以下路径相对项目根目录；行号对应本轮工作区。

## 必须修复的问题

### 1. [P1] 数据同步会先删目标表，再因导入语法错误退出

位置：`docker/scripts/sync-local-db-to-docker.ps1:148`、`:153`、`:154`。

加 `-Apply` 后，脚本先执行 DROP TABLE，再通过 Invoke-Expression 执行包含 `< backup.sql` 的 PowerShell 命令。这个语法在当前 PowerShell 中不成立；在目标连接/删表成功时会留下已删表、未导入的数据库。备份存在并不等于脚本完成自动恢复。

修改要求：移除字符串拼接和 Invoke-Expression，采用参数安全、字节安全的进程标准输入导入；先在临时库验证备份完整可恢复，再应用目标库。禁止先无条件删业务表。增加源目标同实例识别、失败恢复和最终一致性验证。`information_schema.table_rows` 是估算值，不能用它宣布数据内容一致；应比较准确行数、关键数据/哈希和文件内容。同步应明确保留环境专属配置，备份目录加入忽略规则。

### 2. [P1] 管理员改角色后无法通过重新登录恢复使用

位置：`web/backend/src/main/java/com/campus/platform/module/admin/service/AdminPermissionService.java:61`；关联 `AdminUserService.updateAdmin()`、`AuthService.adminLogin()`。

角色变更写入按管理员 ID 存储、没有过期时间的黑名单。登录方法仍签发令牌，但不改变撤销状态；拦截器会拒绝新旧全部令牌。`restoreToken()` 未接入调用链。

修改要求：采用令牌版本或撤销时间与签发时间比较，让旧令牌失效、新登录令牌可用。不要只在登录时清除整个账号黑名单，这会使尚未到期的旧令牌重新有效。增加“改角色→旧令牌拒绝→重新登录→新令牌成功→旧令牌仍拒绝”的完整回归。

### 3. [P1] 失物认领行锁修复仍有旧快照窗口

位置：`web/backend/src/main/java/com/campus/platform/module/lostfound/service/LostFoundService.java:128`、`:139`、`:143`。

在 MySQL REPEATABLE READ 下，事务先普通 SELECT 建立快照，再等待父行锁；取得锁后的普通 selectCount 仍可能读取旧快照。两个事务先后取得锁，并不保证第二个事务的计数看见第一个事务刚提交的认领，因此不能据此宣布消除了重复认领。

修改要求：以父行锁作为业务读取起点，必要时对有效认领使用当前读，并让同意/拒绝/归还等遵循一致锁顺序与状态机。该结论是对现有代码与 REPEATABLE READ 条件的审查；本轮未在真实数据库重现，需用两个独立数据库事务和同步屏障验证，不能用 Mockito 替代。

### 4. [P2] AI 消息增量仍不可靠地刷新，鉴权错误也未统一处理

位置：`web/frontend/student/src/views/ai/ChatView.vue:270`、`:276`、`:292`；`web/frontend/student/src/api/ai.js`。

把普通对象 assistantMsg 放入 ref 数组后，回调仍直接修改原始对象，绕过 Vue 的代理；本轮已用项目 Vue 复现不触发订阅。页面可能直到 asking 等其他状态变化时才显示内容，不能保证逐段响应。

此外 api 层虽能识别 JSON 401/503，但 ChatView 的 onError 只写文本、弹提示，未接入房内登录/维护处理。SSE 读取到 EOF 时仍当成功，也未要求收到 done，截断流可能被当作完整回复。

修改要求：使用响应式消息对象或通过数组中的代理更新；抽取可供 fetch/SSE 复用的统一业务错误策略；中途 EOF 与正常 done 分开处理。测试完成前显示多次增量、JSON 401、503、截断流和切换会话。

### 5. [P1/P2] Docker 配置只修了一部分

位置：`docker-compose.yml:31`、`:60`、`:65`、`:86`。

- 签名密钥已传入，但缺失时仍使用仓库公开的固定默认值，未实现部署缺少密钥时拒绝启动。
- Redis 仍未开启密码，后端仍允许设置 REDIS_PASSWORD，非空时会出现客户端与服务端配置不一致。
- 前端仍依赖 backend 的 service_started；后端健康检查没有完成依赖就绪语义。
- Nginx 上传限制和关闭缓冲已增加，但缺失静态图片仍可能由通用 SPA fallback 返回 HTML 200，未添加资源专用 404 规则。

修改要求：部署必须提供密钥，Redis 服务端/客户端/健康检查统一；补 readiness 与静态资源规则，通过 Docker 实际入口验收。当前 Docker 不可连接，这些运行结果未验证。

### 6. [P2] 系统配置仍在提交前发布到缓存，输入校验未完成

位置：`web/backend/src/main/java/com/campus/platform/module/site/service/SystemConfigService.java:49`、`:70`、`:88`、`:105`、`:113`。

SystemConfigHolder 的整体快照替换已修复半空缓存，但 Service 仍在事务内调用 refresh：其他请求可能读到尚未提交的配置，提交失败也可能留下缓存与库不一致。任意配置值及未知键的校验仍未完善。

修改要求：事务成功提交后刷新；明确配置键、类型、范围和可删除性；验证回滚、非法值、未知键及真实配置消费者。

### 7. [P2] 回答列表操作后分页状态不一致

位置：`web/frontend/student/src/views/qa/Detail.vue:130`、`:144` 附近。

回答/采纳后调用不带分页参数的 questionDetail(id)，返回第一页，但没有同步重置当前页码与总数。在后续页操作会出现页码和实际内容不对应；新增回答后总数也可能仍是旧值。标题使用 answers.length，仅反映当前页条数。

修改要求：统一复用分页加载函数；新增后明确回到目标页并刷新 total，采纳后保留或显式重置页码；标题使用总数。用超过两页回答测试。

### 8. [P2] WebSocket 注册竞争没有修复，新增测试不足以证明真实并发

位置：`web/backend/src/main/java/com/campus/platform/module/chat/websocket/ChatSessionRegistry.java:23`、`:25`、`:37`。

容量判断与添加仍不原子；注销的空集合检查与删除也未与注册互斥。并发连接可能突破上限，注册与注销交错可能丢失可投递集合。新增测试不改变这个实现。

修改要求：以用户维度原子更新注册集合，覆盖并发注册和最后一个注销与新注册交错。单实例与多实例能力分别说明。PostServiceConcurrencyTest 等采用 Mockito，只能证明预期调用，不能证明真实 MySQL 竞争结果。

## 上轮 14 组任务对照

| 项目 | 本轮结论 |
| --- | --- |
| 1 管理员权限 | 配置超级管理员检查和账号状态校验已加入；令牌撤销存在新问题，见 2 |
| 2 公开读取与个性化 | 已改成 GET 白名单并解析有效令牌；仍需匿名详情和房内真实回归 |
| 3 管理端菜单与待办 | visibleGroups 已使用、路由与统计有修改；本轮未做浏览器验收 |
| 4 Docker | 上传上限、缓冲、密钥传入有修改；仍有第 5 条缺口 |
| 5 迁移与同步 | INSERT IGNORE 与 SET NAMES 已补；同步脚本不能使用，见 1 |
| 6 失物认领 | 事务、条件更新、最新申请排序已补；快照竞争未解决，见 3 |
| 7 动态计数 | 已改原子 SQL 增减，取消点赞按影响行数扣减；真实并发仍未验证 |
| 8 分页 | 已增加成员/认领/回答分页及全局上限；回答操作后的页码/总数仍有问题 |
| 9 图片 | 有分类示意图、WebP 和兜底修改；尚需逐模块浏览器视觉及部署字节验收 |
| 10 分享 | 已增加详情端点和目标动态加载；尚需浏览器直达、不可见内容验收 |
| 11 AI | JSON 错误识别和取消已增加；响应式、统一错误、截断流仍有缺口 |
| 12 配置一致性 | 原子快照已改；事务提交时机和校验仍未完成 |
| 13 WebSocket | 注册实现未改；不能认定并发与服务联调完成 |
| 14 验收 | 单元测试增加并通过；真实服务/数据库并发和本轮浏览器回归尚未完成 |

建议顺序：先停用并修复同步脚本，再修管理员撤销、认领事务、AI 和 Docker，最后补足状态分页、配置和真实联调证据。不要把注释中的“已修复”或测试名称中的“并发”当作验收结果。
