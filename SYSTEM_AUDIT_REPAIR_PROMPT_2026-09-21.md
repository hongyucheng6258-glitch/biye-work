# 全系统检查结果与完整修复提示词

检查日期：2026-09-21。项目：`E:\work\毕业设计UI原型\Ai-campus`。

## 一、本次实际验证范围

这是一轮源码审查、构建、自动化测试和模拟接口浏览器回归，不等于全部真实服务已联调成功。本次未修改业务代码，也未修改数据库。

| 检查 | 结果 | 能证明什么 |
| --- | --- | --- |
| 学生端单元测试 | 50 项通过 | 已有测试覆盖的逻辑正常 |
| 管理端单元测试 | 10 项通过 | 已有测试覆盖的逻辑正常 |
| 后端 `mvn clean test` | 193 项通过，0 失败、0 错误、0 跳过，44 份新报告 | 清理旧报告后的本次结果；不是累加历史报告 |
| 学生端、管理端生产构建 | 均通过 | 可构建；仍有大分包提醒 |
| `tests/browser/system-all-rooms.cjs` | 通过 | 10 个服务模块的模拟数据页面挂载；不是每项提交操作都已验证 |
| `tests/browser/system-interactions.cjs` | 通过 | 3D 进房、活动图片、模拟报名、房内聊天、返回房间、全屏与移动端等已覆盖路径 |
| `tests/browser/system-auth.cjs` | 通过 | 已覆盖房内普通请求的 401/403 分流，不代表流式 AI 请求也覆盖 |
| `tests/browser/system-admin.cjs` | 通过 | 已覆盖管理端错误/空状态、重试、部分移动布局 |
| `tests/browser/system-scene-and-fallback.cjs` | 通过 | 编辑器失败降级、取消加载、房内 503、重复进出场景及画质/尺寸切换；生成截图不等于所有视觉细节已人工验收 |
| Docker、真实 MySQL/Redis/AI/上传/WebSocket 联调 | 本轮未完成 | Docker 引擎连接失败，不能声称真实服务或并发压测已通过 |

以下“已确认”指源码中存在对应问题，不表示本轮在真实部署环境中进行了越权或并发操作。部署运行结果、竞争条件的发生频率需要修复时用隔离环境验证。

## 二、主要问题

| 优先级 | 问题 | 证据入口与影响 |
| --- | --- | --- |
| P1 | 管理端系统配置缺少后端超级管理员权限检查 | `AdminSystemConfigController`、`AdminInterceptor`：前端 `superOnly` 不能防止普通管理员直接请求配置接口 |
| P1 | 已删除/停用的管理员仍可能使用未过期令牌 | `AdminInterceptor` 仅验证令牌角色，没有核验当前管理员记录与状态 |
| P1 | 公开页面和接口鉴权不一致；登录用户的公开动态列表丢失个性化状态 | `WebMvcConfig` 排除 `/api/post/list` 导致不解析令牌；`PostService.list` 因用户上下文为空返回未点赞/未收藏 |
| P1 | 配置迁移重复执行覆盖已保存配置 | `migrate_v7_system_config.sql` 的 `ON DUPLICATE KEY UPDATE` 更新配置值，可能重置维护开关、限制和站点配置 |
| P1 | Compose 没传签名密钥；Redis 密码配置不配套 | `.env.example` 有 JWT/签到密钥，Compose 后端未传入；后端 Redis 密码与 Redis 启动配置不一致 |
| P1 | 失物认领存在竞争及部分更新风险 | `LostFoundService.claim/handleClaim/confirmReturn` 缺少完整事务和并发状态保护；`myClaim` 的 `LIMIT 1` 没有排序 |
| P1 | 动态点赞、评论数可能并发丢失更新 | `PostService` 先读取计数、在 Java 中加减、再写整个实体 |
| P1 | AI 流式请求可能把业务 401 当空成功 | `api/ai.js` 只校验 HTTP 状态，不先识别 HTTP 200 下的 JSON 业务错误 |
| P2 | 管理端菜单和待办导航错误 | `AdminLayout.vue` 模板循环 `navGroups` 而不是 `visibleGroups`；`goNotice` 使用带 `/admin` 的逻辑路由，与路由 base 重复 |
| P2 | 分页未完全覆盖且参数约束不统一 | 评论前端只拿前 50 条；部分成员、认领列表全量返回；分页插件未设置统一上限 |
| P2 | 图片兜底语义与真实内容不一致 | 多条闲置共用通用图片；纯文字动态被补上同一校园照片；多图与图片加载失败处理不完整 |
| P2 | 分享链接未定位到目标动态 | `sharePost` 写入 `?post=id`，页面没有对应目标动态加载逻辑 |
| P2 | 配置类型校验和缓存更新不完整 | `SystemConfigService` 接受任意配置值，并在事务提交前刷新缓存；缓存刷新存在中间状态 |
| 待运行验证 | Docker 上传上限与 SSE 代理配置不配套 | Nginx 未明确设置上传上限和流式关闭缓冲；需要通过部署入口验证上传与逐段响应 |
| 扩展项 | WebSocket 注册并发与多实例投递 | 本地集合的容量判断非原子；消息推送只覆盖本进程，不能据此声称支持多副本 |

## 三、可直接复制给开发助手的完整提示词

从下一行开始复制，直到“提示词结束”。

---

请在 `E:\work\毕业设计UI原型\Ai-campus` 中修复并完善现有校园服务系统。先阅读实际代码、仓库规则和本文件，再开始修改。下面路径除特别说明外均相对该项目根目录。

### 目标与必须保留的行为

1. 保留已有学生端、管理端和现代蓝白 3D 校园，不重写成另一套原型，不把真实接口退回演示数据。
2. 3D 房间内手动点击服务屏幕后，原位显示学生端对应业务组件；复用相同接口、图片、校验、权限与交互。关闭内容返回当前房间；离开房间返回对应门口，不能跳到初始大厅。
3. 保留现有 2D 学生页面，不再复制一套 2D 业务。房内打开详情、报名、编辑、聊天、登录提示等都要留在房内工作区。
4. 不删除用户未提交的文件，不清空真实数据库，不执行删除 Docker 数据卷来“解决”启动或同步问题。数据库修改先备份、可回滚；测试使用隔离库和测试账号。
5. 下列条目是检查起点。修改前验证实际调用链；如果已有修复，给出对应文件和测试证据，不能重复引入平行实现。测试通过不能替代业务链路验收。

路径简写：`B = web/backend/src/main/java/com/campus/platform`，`S = web/frontend/student/src`，`A = web/frontend/admin/src`。下文 `B/...`、`S/...`、`A/...` 必须按此展开。

### 1. 先修复服务端管理员权限

修改：
- `B/interceptor/AdminInterceptor.java`
- `B/module/admin/controller/AdminSystemConfigController.java`
- `B/module/admin/controller/AdminSystemController.java`（参考已有超级管理员校验并统一）
- `B/module/admin/service/AdminUserService.java`
- 必要时新增统一的管理员权限组件，接入现有管理员实体、Mapper 和令牌机制。

具体要求：
- 校验令牌后核对管理员是否存在、是否可用，并从当前数据库状态判断业务权限；不能只相信 JWT 内的 `role=admin`。
- 系统配置读写、刷新、删除及管理员管理按现有权限设计执行服务端限制，至少保证普通管理员不能调用超级管理员配置接口。
- 删除、禁用、降权后，旧令牌不能继续执行原权限的操作。优先复用已有撤销机制，不为所有请求随意增加不必要的复杂系统。
- 对所有 `/api/admin/**` 控制器建立权限清单，检查遗漏。角色名称以项目实际定义为准，修正前后端不一致的角色标签。
- 不在响应或日志中返回密码、密钥、完整令牌。统一清理用户上下文。

验收：无令牌、学生令牌、普通管理员、超级管理员、禁用管理员、删除管理员、降权旧令牌分别验证；既验证入口导航，也直接请求后端接口。拒绝请求不得修改数据。

### 2. 统一公开读取和登录态个性化

修改：
- `B/config/WebMvcConfig.java`
- `B/interceptor/JwtInterceptor.java`
- `S/router/routes.js`、`S/router/index.js`
- `B/module/post/controller/PostController.java`、`B/module/post/service/PostService.java`
- 活动、闲置、失物、公告、互助问答、搭子的实际公开列表/详情控制器。

具体要求：
- 用“HTTP 方法 + 明确端点”的规则区分允许匿名读取与必须登录的操作，不用宽泛路径排除放开写接口。
- 公开读取允许无令牌；携带有效学生令牌时仍解析用户上下文，使点赞、收藏等个性化状态正常。
- 前端标记为公开的详情与后端权限一致；匿名只能读取可公开且已通过审核的内容，不泄露私有联系方式或他人资料。
- 无效/过期令牌策略明确并与客户端一致；禁止把管理员身份当作学生身份使用。

验收：匿名列表/详情、匿名提交被拒绝、登录点赞后刷新保持状态、收藏后刷新、未审核或已删除详情、403 不误清登录态、401 正常引导登录；在 2D 和房内各回归一次。

### 3. 修复管理端导航、菜单及待办数

修改：`A/layout/AdminLayout.vue`、`A/router/index.js`、对应待办统计接口及其 API 封装。

具体要求：
- 模板使用经过角色过滤的 `visibleGroups`，不要继续循环未过滤的 `navGroups`。
- `goNotice()` 使用 `/audit/activity` 等 router 内部路径，不能再把 `createWebHistory('/admin/')` 的 base 拼入逻辑路由。
- 对损坏的 `admin_info` 本地 JSON 做容错处理，不能令全局导航守卫崩溃。
- 明确待办统计口径；AI 待办如果是业务待审的子集，不能与业务数直接相加重复计数。
- 使用后端准确汇总，不只读取前 100 条推算总数；接口失败显示可重试的失败状态，不能显示成 0 条。

验收：不同角色菜单可见性；点击每类待办跳转到正确页面；超过 100 条时统计准确；接口失败、重试、空数据分别验证。

### 4. 修复 Docker 启动与部署配置

修改：`docker-compose.yml`、`.env.example`、`web/backend/src/main/resources/application.yml`、`docker/nginx/student.conf`、`docker/nginx/admin.conf`、`README.md`。

具体要求：
- 将 JWT 和签到签名密钥明确传入后端容器；部署配置缺少必要密钥时给出清楚的启动错误，不悄悄使用代码里的固定默认密钥。开发测试可用专属测试配置。
- Redis 服务端、后端客户端和健康检查采用一致的密码配置。不能只给客户端设置密码而 Redis 不启用认证。
- 区分进程存活与依赖就绪；添加合适的 readiness 检查，Compose 对后端就绪的依赖与实际检查含义一致，避免“容器启动=系统可用”。
- Nginx 上传大小与后端图片/PDF/请求总量限制配套，前端提示正确处理 413；检查两套前端入口。
- 对实际 AI SSE 端点关闭不适合的代理缓冲，设置合理超时，验证浏览器能在请求结束前收到多段内容。
- 静态图片缺失应返回真实 404，不因 SPA fallback 返回 HTML 200；页面路由仍保留 SPA fallback。
- 区分重建镜像和仅重启容器。可加入非敏感构建版本信息，用响应内容类型、文件签名/哈希和版本确认部署的是最新图片与前端。

验收：启动日志无秘密泄露；MySQL/Redis/backend 健康；学生端、管理端可访问；重启数据仍在；通过 Nginx 上传超过 1MB 的合法图片与合法 PDF；非法/超限文件被明确拒绝；SSE 逐段响应；WebSocket 经代理连接成功。

### 5. 修复数据库迁移和本地到 Docker 同步

修改：
- `web/backend/src/main/resources/db/migrate_v7_system_config.sql`
- `docker/mysql/02-app-extension.sql` 及 Compose 挂载的数据库初始化脚本
- 新增 `docker/scripts/sync-local-db-to-docker.ps1`（若存在同功能脚本则完善它）
- 新增数据库备份、恢复、同步说明。

具体要求：
- 初始化默认配置只补缺失项，不能每次执行覆盖用户保存的 `config_value`。把需要纠正的历史数据作为单独、版本化、条件明确的迁移处理。
- 每个独立 SQL 执行入口明确 UTF-8/utf8mb4；检查库、表、列、连接、导入文件和 HTTP 输出编码。
- 乱码不能用重置全部站点设置掩盖。先比较本地原始值和 Docker 值，再修复确定损坏的字段；输出修复前后差异摘要。
- 同步来源必须是用户当前本地数据库，不是初始化 seed SQL；同步目标必须明确是 Docker 数据库。
- 同步脚本先做源/目标备份、表结构和数据差异预览；支持只检查模式，失败立即停止。备份以字节安全方式生成，避免 Windows 文本管道转码。
- 检查业务表、用户、报名、消息、配置和实际文件存储方式；图片若为数据库二进制也要验证，不能只比 URL。
- 对存在冲突的数据明确规则，保留环境专属配置；不在仓库提交数据库备份或真实秘密。无法确认源实例时停止同步操作并说明，而不是用演示数据顶替。

验收：重复迁移不覆盖自定义值；中文、emoji、页面标题正确；同步前后表行数、关键记录、关联关系和文件哈希有报告；恢复流程可用。

### 6. 修复失物认领状态机与并发

修改：`B/module/lostfound/service/LostFoundService.java`、`B/module/lostfound/mapper/LostFoundClaimMapper.java`、相关失物 Mapper 和必要数据库迁移。

具体要求：
- 针对 `claim()` 的“先计数再插入”使用统一事务和数据库级并发保护；以当前业务规则确认每个物品允许的有效认领数量。
- `handleClaim()`、`confirmReturn()` 按同一状态机做带旧状态条件的更新；同意、拒绝、归还等关联更新保持一致，重复点击幂等。
- 如果用父记录行锁，所有相关状态变更采用一致锁顺序；通知以可靠方式在业务提交后发送，不能留下半完成数据。
- `myClaim()` 按当前用户和明确顺序取最新申请，不能无序 `LIMIT 1` 返回历史拒绝记录。

验收：多人同时认领、重复点击、同时同意/拒绝、重复确认归还、拒绝后再次申请、模拟中途失败；使用真实隔离 MySQL 检查最终状态与消息一致性。

### 7. 修复动态计数竞争

修改：`B/module/post/service/PostService.java`、`B/module/post/mapper/PostMapper.java` 及点赞/评论相关 Mapper、约束迁移。

具体要求：
- 把读取旧值后 `updateById(post)` 的计数更新改为数据库原子增减或等效一致性方案；避免写整个旧实体覆盖其他并发字段。
- 点赞关系唯一约束、重复点赞幂等、取消点赞不存在关系时不扣数；根据实际影响行数决定是否改计数。
- 评论新增、删除/隐藏、审核状态变化的统计口径一致，计数不得负数；必要时提供校正历史计数的独立脚本。

验收：不同用户同时点赞/评论、同用户重复提交、点赞取消交错，最终计数与有效明细行数一致。

### 8. 完成内外两层分页与参数保护

修改：`B/config/MybatisPlusConfig.java`、各模块列表请求参数/Controller、`S/components/CommentList.vue`、`S/views/social/PostSquare.vue`、`B/module/activity/controller/ActivityController.java`、`B/module/activity/service/ActivityService.java`、`S/views/activity/Detail.vue`、`S/views/activity/MySignup.vue`，以及认领、成员等实际全量列表的 API/组件。

具体要求：
- 建立统一分页输入规则，例如页码至少 1、页大小 1～100，并设置分页插件合理上限。对负数、0、超大数明确处理，不能形成无界查询。
- 外层活动/闲置/搭子/失物/问答/动态/公告/消息等列表和内层评论、回答、报名成员、认领记录分别检查；“只取前 50 条”不是分页。
- 对需要分页的全量接口返回标准分页结构，同时修改所有消费者。AI 会话列表等增长型列表也检查分页或增量加载。
- 保留搜索、分类、排序；条件变化重置页码，返回详情恢复原列表，删除末页最后一项自动回退有效页。
- 小规模固定房间导航不必为了“两层分页”强制分页。

验收：用超过两页的数据逐页读取，记录无重复无丢失；空页、越界、搜索后页码和末页删除；2D 与房内一致；检查实际 SQL 有有效 LIMIT。

### 9. 修复图片完整性与显示语义

修改：`S/utils/content-assets.mjs`、`S/utils/content-assets.test.mjs`、`S/components/ItemCard.vue`、`S/views/social/PostSquare.vue`、`S/features/campus3d/campus-data.js`、`S/features/campus3d/campus-scene.js`，以及这些工具的列表/详情调用点。

具体要求：
- 真实上传图片优先，完整保留所有有效图片，兼容当前 API 实际支持的数组/序列化形式；不要只保留第一张。
- URL 非空但加载失败时也有兜底，防止递归报错；列表、详情、预览和 3D 内容使用同一规则。
- 无图闲置按已知物品类别选择对应示意图，不能给所有物品配一张包含多种物品的图冒充实物。示意图明确标注，纯文字动态正常显示文字，不强行补上校园照片。
- 若确需生成缺失图片，先列出物品与资产的准确对应关系，再生成；不替换用户真实上传图。
- 合并重复的服务图片映射。`roomScreen()` 替换图片材质后仍保持房间名称及“点击打开服务内容”可读，不让海报遮住操作提示。
- 优化当前体积较大的 PNG，采用合理尺寸、WebP/缩略图、懒加载及宽高占位；保留原图查看能力。

验收：有图、无图、损坏地址、多图、不同类别物品、纯文字动态、图片预览；逐一对比 2D 与房内内容；部署入口核验图片 MIME 和真实字节。

### 10. 修复动态分享定位

修改：`S/views/social/PostSquare.vue`、对应动态 API 文件、`B/module/post/controller/PostController.java`、`B/module/post/service/PostService.java`。

具体要求：
- `?post=id` 能加载并展示目标动态，即使它不在列表第一页；优先复用合理详情展示，缺少端点时增加只返回可见动态的详情接口。
- 支持刷新、直达链接和房内导航；找不到、删除、审核不可见时有明确提示。
- 评论分页和返回列表状态与普通入口一致，不额外整页跳转破坏 3D 工作区。

### 11. 修复 AI 流式错误、取消和会话隔离

修改：`S/api/ai.js`、`S/views/ai/ChatView.vue`、现有统一请求/鉴权策略模块；必要时协调 `B/module/ai/controller/AiController.java`、`B/module/ai/service/AiChatService.java`、`B/module/ai/gateway/AiGatewayService.java`。

具体要求：
- `chatStream()` 先区分 SSE 与 JSON；HTTP 200 中 `code=401/403/503` 等业务错误必须进入统一错误策略，不能到 EOF 就当成功。
- 校园向导分支复用已有 API 封装和错误策略，避免裸 `fetch` 绕过房内登录处理。
- 使用 AbortController 与请求标识，在离开组件、关闭房内内容、切换会话时中止或隔离旧响应；迟到数据不能写进新会话。
- 消息增量更新必须触发 Vue 响应式渲染；验证第一段、后续段在结束前显示，不能只在最终完成时出现。
- SSE 分片、跨 chunk 行、CRLF、error/done、网络断开和取消分别处理，释放 reader 和事件资源。

验收：模拟 HTTP 200+JSON 401、HTTP 403/500、SSE error、空响应、分片内容、流中断、连续切会话、关闭后重开；真实 AI 最小调用验证放在已配置凭据的隔离环境，没配置就明确未验证。

### 12. 校验系统配置并保证缓存一致

修改：`B/module/site/service/SystemConfigService.java`、`B/config/SystemConfigHolder.java`、配置 DTO/管理页面及实际消费相关配置的服务。

具体要求：
- 已知配置键定义类型、范围及是否可删除；布尔值、数字限制、URL 等拒绝非法值，不能任意转换失败后悄悄回退。
- 批量更新不应偷偷新增未知键；如果保留自定义配置功能，通过独立受控入口创建并校验。
- 事务提交后刷新缓存，以完整不可变快照或等效原子替换保证读取不会看见“先 clear 后逐项填充”的中间状态。
- 配置更新失败不改变缓存；梳理每个可编辑配置的实际消费者。页面宣称可调整的上传数量、大小等限制应真正生效，未支持的选项不能伪装为已生效。

验收：非法输入无写入；事务回滚不污染缓存；并发读取不中途缺键；修改关键限制后真实接口行为变化，恢复配置后还原。

### 13. 检查 WebSocket 与扩展能力

修改：`B/module/chat/websocket/ChatSessionRegistry.java`、`B/module/chat/websocket/ChatRealtimePublisher.java`，及实际握手、票据、会话与消息持久化调用链。

具体要求：
- 注册连接的数量判断与添加保持原子；注销时不能把刚加入新连接的集合误删。
- 保留已有事务提交后推送机制；数据库消息是事实来源，断线重连可补齐历史，重复事件不重复显示。
- 验证票据过期/重放、非会话成员访问、同用户多窗口、离线未读、断线重连和代理连接。
- 当前若只部署单实例，先把单实例正确性做好并写清限制；如目标包含多实例，再使用 Redis 消息通道等实现跨实例投递，并增加双实例测试，不把内存 registry 宣称为多实例方案。

### 14. 建立完整可复现的回归与联调报告

修改/新增：`tests/browser/system-all-rooms.cjs`、`system-interactions.cjs`、`system-auth.cjs`、`system-admin.cjs`、`system-scene-and-fallback.cjs`、相关前后端单元测试；新增独立真实集成测试目录和运行说明。

具体要求：
- 浏览器地址、浏览器工具路径、测试账号和测试输出目录参数化，不依赖个人机器固定路径。
- 房间测试不仅检查存在一个 DOM 子节点，要断言目标模块、实际数据、按钮操作、提交后的状态与返回路径。
- 明确区分 mock 浏览器测试、真实数据库测试、真实服务联调。所有真实写操作限定测试数据，记录并清理本次创建的 ID。
- 并发压测使用隔离数据库，报告并发数、持续时间、成功/业务拒绝/系统错误比例、P95、数据库最终一致性；不能只给请求成功率。
- 构建测试从干净报告开始计数。失败修复后再验收，不能删除断言或跳过测试来获得通过。

按下面业务清单逐项给“通过/失败/未验证”，每项包含入口、操作、预期、实际、证据：

| 模块 | 必测业务链路 |
| --- | --- |
| 登录与权限 | 注册/登录/退出、过期、被禁用、401/403、角色越权、他人记录操作 |
| 活动 | 列表/详情/图片、筛选分页、报名/取消、重复报名、名额竞争、已有编辑/审核/签到链路 |
| 闲置 | 发布多图、详情、编辑权限、审核、收藏、私聊、已有完成/评价流程 |
| 学习搭子 | 发布、筛选、详情、编辑、联系和已有状态变更 |
| 失物招领 | 发布/审核、认领、同意/拒绝、再次申请、确认归还与并发 |
| 互助问答 | 提问/编辑、答案、采纳权限、点赞、搜索和两层分页 |
| 动态 | 发布多图/纯文字、审核、点赞/取消、收藏、评论、分享直达、举报 |
| 公告 | 发布/编辑/审核或上下架、详情、筛选分页、学生可见性 |
| 消息与聊天 | 未读、已读、历史分页、实时消息、重连补齐、越权、房内打开 |
| AI 与学习工具 | 流式对话、会话切换/删除、向导、PDF 上传问答、代码纠错、错题/识别等现有功能 |
| 上传与资源 | 图片/PDF、类型/大小、错误反馈、鉴权、预览、重启后可读 |
| 管理端 | 每类审核/操作、统计、举报、用户管理、权限、配置保存和实际生效 |
| 3D | 加载/降级、全屏、地图隐藏、每扇门、手动开服务、房内详情/表单、Esc 分层关闭、返回门口、移动布局、反复进入资源释放 |
| 部署与数据 | 健康状态、中文编码、重启持久化、最新静态资源、数据库同步与恢复 |

### 执行顺序与交付

先完成权限和数据安全问题，再修并发、分页和 AI，再处理图片、导航、部署和完整联调；有依赖的数据库迁移先备份并在隔离环境验证。

每一阶段交付：修改文件及函数、问题原因、行为变化、测试命令和实际结果。最终交付：
1. 可运行代码和必要的幂等迁移。
2. 更新的 README，包含 Docker、配置、同步、备份恢复及测试步骤。
3. 完整验收矩阵，区分已验证和环境阻塞；未运行的真实 AI/Redis/MySQL/WebSocket 不得写“全部通过”。
4. 遗留问题与原因，说明是否影响当前单机演示或仅影响未来扩展。

本任务结束时不要自行推送代码或删除旧数据。给出可审查的改动与证据；仓库上传按用户另行要求执行。

提示词结束。
