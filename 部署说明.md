# AI校园综合服务平台 - 部署说明

本说明覆盖从零部署的完整流程：数据库、Redis、后端、学生端、管理端，以及常见问题排查。

## 一、环境要求

| 组件 | 版本要求 | 说明 |
|------|----------|------|
| JDK | 17 或更高 | 后端运行环境 |
| Maven | 3.8 或更高 | 后端构建 |
| Node.js | 18 或更高 | 前端构建 |
| npm | 随 Node.js 安装 | 前端依赖管理 |
| MySQL | 8.0 | 业务数据库（图片以 Base64 入库） |
| Redis | 6.0 或更高 | 验证码、会话、限流 |
| MinIO | 可选 | 仅兼容旧接口，图片已改为数据库存储 |
| Docker Engine | 可选 | Docker Compose v2，用于容器化部署 |
| DeepSeek API Key | 按需配置 | 也可在管理后台「AI 配置」页在线配置 |

### 1.1 Docker Compose 快速部署

仓库根目录的 `docker-compose.yml` 会启动 MySQL、Redis、后端、学生端和管理端。首次创建 `mysql_data` 数据卷时会初始化数据库并导入演示数据；已有数据卷不会重复执行初始化脚本。

```powershell
Copy-Item .env.example .env
notepad .env
```

在本机 `.env` 中填写 `MYSQL_ROOT_PASSWORD`、`REDIS_PASSWORD`、`JWT_SECRET` 和 `SIGNIN_SECRET`。四项不能为空；JWT 密钥至少 32 个字符，签到密钥至少 16 个字符。可以在本机运行 `openssl rand -hex 32` 生成随机值。`AI_API_KEY` 按需填写。`.env` 不要提交到仓库。

```powershell
docker compose up -d --build
docker compose ps
```

启动后访问学生端 `http://localhost:5173`、管理端 `http://localhost:5174/admin/`、后端 `http://localhost:8080`。查看后端日志：`docker compose logs -f backend`。

```powershell
docker compose down
```

`docker compose down` 会保留数据库卷；`docker compose down -v` 会删除容器数据。生产部署前，请从 Compose 配置移除 `reset_and_testdata.sql` 演示数据挂载，并使用单独的最小权限数据库账号和密钥管理方案。
## 二、数据库部署

### 2.1 创建数据库

```sql
CREATE DATABASE ai_campus_platform DEFAULT CHARACTER SET utf8mb4 COLLATE utf8mb4_general_ci;
```

### 2.2 初始化表结构

执行 `web/backend/src/main/resources/db/schema.sql`：

```bash
mysql -u root -p ai_campus_platform < web/backend/src/main/resources/db/schema.sql
```

### 2.3 执行迁移脚本（按顺序）

```bash
mysql -u root -p ai_campus_platform < web/backend/src/main/resources/db/migrate_v2_wrongbook.sql
mysql -u root -p ai_campus_platform < web/backend/src/main/resources/db/migrate_v3_wrongbook_analyze.sql
mysql -u root -p ai_campus_platform < web/backend/src/main/resources/db/migrate_v4_wrongbook_generated.sql
mysql -u root -p ai_campus_platform < web/backend/src/main/resources/db/migrate_v5_images_to_db.sql
mysql -u root -p ai_campus_platform < web/backend/src/main/resources/db/migrate_v6_all_uploads_to_db.sql
mysql -u root -p ai_campus_platform < web/backend/src/main/resources/db/migrate_v7_system_config.sql
mysql -u root -p ai_campus_platform < web/backend/src/main/resources/db/2026-08-05-chat-blocker-high-migration.sql
mysql -u root -p ai_campus_platform < web/backend/src/main/resources/db/2026-08-07-ai-content-audit-migration.sql
mysql -u root -p ai_campus_platform < web/backend/src/main/resources/db/migrate_v8_draw_guess.sql
mysql -u root -p ai_campus_platform < web/backend/src/main/resources/db/migrate_v9_draw_guess_artwork.sql
```

> - `2026-08-07-ai-content-audit-migration.sql` 为闲置/活动/失物/动态四类 UGC 增加 AI 审核字段（`ai_risk_level` / `ai_audit_reason` / `ai_audit_time` / `audit_source`）。
> - `migrate_v8_draw_guess.sql` 幂等地新增四张你画我猜业务表；已有数据库随后执行一次 `migrate_v9_draw_guess_artwork.sql`，为回合表增加作品数据列。新安装的 `schema.sql` 与 Docker 初始化扩展已包含 v8、v9 所需结构。
> - 学习搭子（`study_partner`）、互助问答（`campus_question` / `campus_answer`）等社交模块表由应用启动时自动建表，无需手工执行。

### 2.4 导入测试数据（可选）

```bash
mysql -u root -p ai_campus_platform < web/backend/src/main/resources/db/reset_and_testdata.sql
```

内置账号（密码均为 `admin123`）：

| 类型 | 账号 |
|---|---|
| 学生 | `2021001` 张三、`2021002` 李四、`2021003` 王五、`2021004` 赵六、`2021005` 陈七 |
| 管理员 | `admin`（超级管理员）、`auditor`（审核员） |

### 2.5 MySQL 配置建议

图片以 Base64 入库，需要调大包大小：

```ini
[mysqld]
max_allowed_packet=128M
```

## 三、Redis 部署

```bash
redis-server
```

验证：

```bash
redis-cli ping
# PONG
```

默认端口 6379。生产环境建议设置密码，并通过 `REDIS_PASSWORD` 环境变量注入。

## 四、后端部署

### 4.1 配置环境变量

以下变量覆盖 `application.yml` 默认值，生产环境务必修改：

| 环境变量 | 说明 | 默认值 |
|----------|------|--------|
| `MYSQL_HOST` | MySQL 主机 | localhost |
| `MYSQL_PORT` | MySQL 端口 | 3306 |
| `MYSQL_DB` | 数据库名 | ai_campus_platform |
| `MYSQL_USER` | 数据库用户 | root |
| `MYSQL_PASSWORD` | 数据库密码 | 必填；仅通过本地环境变量配置 |
| `REDIS_HOST` | Redis 主机 | localhost |
| `REDIS_PORT` | Redis 端口 | 6379 |
| `REDIS_PASSWORD` | Redis 密码 | 必填；仅通过本地环境变量配置 |
| `MINIO_ENDPOINT` | MinIO 地址 | http://localhost:9000 |
| `MINIO_ACCESS_KEY` | MinIO 账号 | 仅启用兼容接口时在本地配置 |
| `MINIO_SECRET_KEY` | MinIO 密钥 | 仅启用兼容接口时在本地配置 |
| `AI_BASE_URL` | AI 服务地址 | https://api.deepseek.com |
| `AI_API_KEY` | AI 服务密钥 | 按需配置；不要写入源码或文档 |
| `AI_MODEL` | AI 模型名 | deepseek-chat |
| `JWT_SECRET` | JWT 签名密钥 | 必填；至少 32 个随机字符 |
| `SIGNIN_SECRET` | 活动签到 HMAC 密钥 | 必填；至少 16 个随机字符 |
| `TRUSTED_ORIGINS` | 可信来源（逗号分隔） | http://localhost:5173,http://localhost:5174 |

### 4.2 构建与启动

```bash
cd web/backend
mvn clean package -DskipTests
java -jar target/platform-server.jar
```

开发环境可直接运行：

```bash
cd web/backend
mvn spring-boot:run
```

后端默认端口 `8080`，接口统一前缀 `/api`。

### 4.3 AI 配置

AI 密钥两种配置方式：

1. **环境变量**：`AI_API_KEY`、`AI_BASE_URL`、`AI_MODEL`
2. **管理后台「AI 配置」页**：写入 `ai_config` 表，优先级高于环境变量，可在线生效

AI 能力清单（场景）：智能对话、PDF 问答、代码修复、错题整理/讲解/复习/出题、AI 辅助发布、校园向导、失物匹配、闲置估价、搭子匹配、问答参考、活动推荐、内容分级审核。

### 4.4 内容审核开关

管理后台「系统配置」可控制：AI 审核开关、高/中风险词（本地规则即时生效）。开启后，闲置/活动/失物/动态/搭子发布时自动 AI 分级：低风险直接通过，中高风险转人工。

### 4.5 验证

```bash
curl http://localhost:8080/api/announcements
```

返回 JSON 数据即正常。

## 五、学生端部署

### 5.1 安装依赖

```bash
cd web/frontend/student
npm install
```

### 5.2 开发模式

```bash
npm run dev
```

默认端口 `5173`（strictPort），已配置 `/api`、`/ws` 代理到后端 8080。

### 5.3 生产构建

```bash
npm run build
```

构建产物在 `dist/`，可由 Nginx 等托管。

### 5.4 Nginx 参考配置

```nginx
server {
    listen 80;
    server_name your-domain.com;

    # 允许 unload/pagehide 事件，避免新版 Chrome 控制台输出
    # "[Violation] Permissions policy violation: unload is not allowed in this document."
    add_header Permissions-Policy "unload=(self)";

    location / {
        root /path/to/student-dist;
        try_files $uri $uri/ /index.html;
    }

    location /api {
        proxy_pass http://localhost:8080;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }

    # 实时聊天 WebSocket
    location /ws {
        proxy_pass http://localhost:8080;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
    }
}
```

## 六、管理端部署

### 6.1 安装依赖

```bash
cd web/frontend/admin
npm install
```

### 6.2 开发模式

```bash
npm run dev
```

默认端口 `5174`（strictPort，被占用直接报错而非换端口），`/api` 代理到后端 8080。

### 6.3 生产构建

```bash
npm run build
```

构建产物在 `dist/`。Nginx 配置与学生端类似：

```nginx
server {
    listen 80;
    server_name admin.your-domain.com;

    add_header Permissions-Policy "unload=(self)";

    location / {
        root /path/to/admin-dist;
        try_files $uri $uri/ /index.html;
    }

    location /api {
        proxy_pass http://localhost:8080;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }
}
```

## 七、测试

```bash
cd web/backend && mvn test
cd web/frontend/student && npm test
cd web/frontend/admin && npm test
```

涉及数据库、Redis 或 AI 服务的测试需准备对应的本地服务。

## 八、常见问题

### 8.1 后端启动报数据库连接失败

检查 MySQL 是否启动、端口/账号/密码是否匹配、`max_allowed_packet` 是否已调大至 128M。

### 8.2 Redis 连接失败

确认 Redis 已启动、端口 6379 未被占用；设置了密码需配置 `REDIS_PASSWORD`。

### 8.3 AI 功能不可用

检查 `AI_API_KEY` 环境变量，或在管理后台「AI 配置」页填入有效 Key（页面配置优先级更高）。

### 8.4 图片上传失败

图片以 Base64 入库，确认 MySQL `max_allowed_packet=128M`、后端 `max-request-size` 已调整。

### 8.5 WebSocket 连接失败

聊天依赖 WebSocket。开发环境 Vite 已代理 `/ws`；生产环境需 Nginx 配置 WebSocket 代理（见 5.4）。

### 8.6 跨域问题

后端通过 `TRUSTED_ORIGINS` 白名单限制来源，生产环境将前端域名加入白名单（含管理端）。

### 8.7 登录验证码不显示 / 校验失败

验证码由后端生成并存入 Redis（图形验证码与数字运算验证随机切换）。检查 Redis 连接；如 Redis 密码错误或未配置，验证码接口会失败。

### 8.8 注册人机检测不通过

注册页内置人机检测（行为校验），请勿使用自动化脚本注册；正常浏览器操作不受影响。

### 8.9 内容一直处于「待审核」

确认管理后台「系统配置」中 AI 审核开关与风险词配置；未开启 AI 时低风险内容由本地规则自动放行，中高风险需人工审核。

### 8.10 管理端端口 5174 打开却显示学生端

vite 已启用 `strictPort`：若 5174 被其他进程占用会直接启动失败。排查占用：`netstat -ano | findstr :5174`，释放端口后重启。

### 8.11 MinIO 上传报 SignatureDoesNotMatch

`minio.access-key` / `minio.secret-key` 必须与启动 MinIO 时的 `MINIO_ROOT_USER` / `MINIO_ROOT_PASSWORD` 一致。
